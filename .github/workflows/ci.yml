name: CI – Clinical Trial Suite

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install ruff
        run: pip install ruff>=0.4

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

  test:
    name: Clinical Trial Suite (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip setuptools wheel
          pip install -e ".[dev]"

      - name: Run clinical trial suite
        run: |
          pytest tests/ -v --tb=short \
            --hypothesis-seed=0 \
            --junitxml=test-results/junit.xml
        env:
          LUMINARK_MODE: local

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.python-version }}
          path: test-results/
          retention-days: 7

  governance-gate:
    name: Self-Governance (LUMINARK governs itself)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip setuptools wheel
          pip install -e ".[dev]"

      - name: Run governance gate on core modules
        run: |
          python luminark_gate.py status
          python luminark_gate.py report luminark/sap_lyapunov.py
          python luminark_gate.py report luminark/sap_geometry_engine.py
        env:
          LUMINARK_MODE: local

  build-docker:
    name: Build & Test Docker Sandbox
    runs-on: ubuntu-latest
    needs: governance-gate
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker sandbox image
        run: docker build -t luminark-sandbox:latest .

      - name: Test sandbox (bypass entrypoint for CI)
        run: |
          docker run --rm \
            --network none \
            --memory 256m \
            --cpus 0.5 \
            --entrypoint python \
            luminark-sandbox:latest \
            -c "print('sandbox operational')"

      - name: Run governance smoke test
        run: |
          pip install -e ".[dev]" -q
          echo "print('hello')" > /tmp/test_pass.py
          python luminark_gate.py govern /tmp/test_pass.py --mode docker
